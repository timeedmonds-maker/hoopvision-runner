name: Read Failed Toronto Local Evidence
# diagnostic-trigger: passer-geometry
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/read-failed-local.yml'
permissions:
  contents: read
  id-token: write
env:
  PROJECT_ID: project-d88c48c6-7f48-4c33-928
  ZONE: asia-southeast1-c
  INSTANCE: hv-dev-l4
  RUN_ID: public-35500155486-1-9091f1475b0c
  WIF_PROVIDER: projects/234135421963/locations/global/workloadIdentityPools/github-actions/providers/github-hoopvision-runner
  DEV_PUBLISH_SA: hoopvision-github@project-d88c48c6-7f48-4c33-928.iam.gserviceaccount.com
jobs:
  read:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.DEV_PUBLISH_SA }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Read continuity and action evidence
        shell: bash
        run: |
          set -euo pipefail
          ROOT="/srv/hoopvision/runs/$RUN_ID/artifacts"
          gcloud compute ssh "$INSTANCE" --zone "$ZONE" --project "$PROJECT_ID" --quiet --command="
            set -e
            echo '=== OBS 8.7032 ==='
            sudo grep '8.7032' '$ROOT/identity_segments/uniform_observations.csv' || true
            echo '=== OBS 9.3038 ==='
            sudo grep '9.3038' '$ROOT/identity_segments/uniform_observations.csv' || true
            echo '=== CONTINUITY GROUP 1/2 ==='
            sudo awk -F, 'NR==1 || \$2==1 || \$2==2' '$ROOT/sam31/continuity_groups.csv'
          "
