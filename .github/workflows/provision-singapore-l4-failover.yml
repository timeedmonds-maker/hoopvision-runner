name: Deprecated Singapore L4 Failover Provisioner

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "DEPRECATED: this stale provisioning workflow is disabled."
          echo "Use retry-l4-capacity-now.yml for bounded live capacity checks."
          echo "When capacity exists, attest the prepared host and dispatch the exact candidate explicitly."
          echo "HOOPVISION_DEPRECATED_PROVISIONER_OK"
