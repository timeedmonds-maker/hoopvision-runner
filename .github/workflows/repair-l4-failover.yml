name: Deprecated L4 Failover Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "DEPRECATED: automated failover-repair mutation is disabled by the 25 Sep integrated delivery plan."
          echo "Use the bounded capacity probe / prepared-host workflow and explicit current-candidate dispatch."
          echo "HOOPVISION_DEPRECATED_FAILOVER_REPAIR_OK"
