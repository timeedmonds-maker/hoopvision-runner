name: Diagnose Toronto Oracle Misses

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/diagnose-toronto-oracle-misses.yml'
      - 'scripts/diag_toronto_oracle_misses.py'

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: project-d88c48c6-7f48-4c33-928
  ZONE: asia-northeast1-b
  INSTANCE: hv-dev-l4-tokyo
  RUN_ID: public-36102119528-1-8cce8f74deff
  WIF_PROVIDER: projects/234135421963/locations/global/workloadIdentityPools/github-actions/providers/github-hoopvision-runner
  CONTROL_SA: hoopvision-github@project-d88c48c6-7f48-4c33-928.iam.gserviceaccount.com

jobs:
  diagnose:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.CONTROL_SA }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Copy diagnostic
        run: gcloud compute scp scripts/diag_toronto_oracle_misses.py "$INSTANCE:/tmp/diag_toronto_oracle_misses.py" --zone "$ZONE" --project "$PROJECT_ID" --quiet
      - name: Inspect oracle misses
        run: gcloud compute ssh "$INSTANCE" --zone "$ZONE" --project "$PROJECT_ID" --quiet --command="sudo python3 /tmp/diag_toronto_oracle_misses.py /srv/hoopvision/runs/$RUN_ID/artifacts"
